{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "chat",
  "description": "Chat window: conversation history and text input. Only the get_app_status command is exposed.",
  "windows": ["chat"],
  "permissions": ["core:default", "allow-get-app-status"]
}
