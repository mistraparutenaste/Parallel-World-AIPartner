{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "chat",
  "description": "Chat window can read the current application status.",
  "local": true,
  "windows": ["chat"],
  "permissions": ["allow-get-app-status"]
}
