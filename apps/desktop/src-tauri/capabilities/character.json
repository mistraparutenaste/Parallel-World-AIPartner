{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "character",
  "description": "Character window: transparent Live2D surface. Drag, model manifest, click-through toggle and speech playback reporting only; no settings writes, no shell, no fs.",
  "windows": ["character"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "allow-get-character-manifest",
    "allow-set-click-through",
    "allow-set-speech-playback"
  ]
}
