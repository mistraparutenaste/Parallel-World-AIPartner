{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "settings",
  "description": "Settings window: diagnostics and configuration. Exposes app status, character manifest and expression/motion control.",
  "windows": ["settings"],
  "permissions": [
    "core:default",
    "allow-get-app-status",
    "allow-get-character-manifest",
    "allow-set-expression",
    "allow-start-motion"
  ]
}
