# 作業内容

## 2026-07-26 設定画面の簡略化

- メイン画面を「設定 / 性格 / 記憶 / チャット」に整理し、設定カテゴリを「AI / ログ / キャラ / 会話 / UI」に再構成。
- 記憶画面を6セクションへ分割し、シークレットモード、タスクCRUD、保存済みメモリー編集、CSV入出力、SQLiteバックアップ、使用量、削除、会話保持件数を集約。
- メモリー本文の選択時取得、CAS更新、履歴記録、CSVのRFC 4180入出力とドメイン保持を実装。
- 人間向けの「あなたについて」をチャットプロンプトから分離して実装し、起動時・終了時・手動更新に対応。
- 要約済み会話だけを対象に、既定30件を残す日次保持処理を追加。
- Irodoriの全成果物判定と、別コンソールで既存導入スクリプトを起動する操作を追加。
- 性格プリセット4種と、ダーク傾向・セーフワード・免責文の「高度な設定」折りたたみを追加。
- Rust/TypeScript契約、Tauriコマンドマニフェスト、設定画面ACLを同期。

### 検証

- `cargo fmt --all --check`
- `cargo clippy --workspace --all-targets --all-features -- -D warnings`
- `cargo test --workspace`
- `corepack pnpm typecheck`
- `corepack pnpm -r test`
- `corepack pnpm build`
- `cargo run -p pw-contracts --bin export-bindings`
