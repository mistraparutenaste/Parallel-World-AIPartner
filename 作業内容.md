# 作業内容

## 2026-07-13 Phase 5 Task 3 allocator fallback修正

- DB open/lock/破損時は高位ビット領域（`>= 2^63`）のプロセス単調allocatorへ縮退し、submit queueを継続する。
- durable IDは低位領域、fallback IDは高位領域という非衝突契約をテストし、DB復旧後も相関IDが衝突しない。
- turn sequenceをconversation外の非cascadeテーブルへ分離し、履歴削除後の同conversation再作成でもIDを再利用しない。
- migration v4で既存conversation sequenceを分離テーブルへ移行する。

## 2026-07-13 Phase 5 Task 3 再レビュー修正

- SQLite永続sequenceで送信前にturn IDをtransaction予約し、cancel/error/restart後もUI相関IDを再利用しない。
- migration v2は既存重複をmessage id順に決定的renumberしてから一意索引を作成する。
- migration v3でconversation単位の`next_turn_id`を追加し、既存最大turn+1へ初期化する。
- 同一turn IDへ異内容をretryした場合は明示的storage errorを返す。

## 2026-07-13 Phase 5 Task 3レビュー修正

- 完了turn保存をrepository portへ移し、conversation upsert + user + assistantを単一transaction・冪等保存に変更。
- migration v2で `(conversation_id, turn_id, role)` 一意索引を追加し、失敗時rollbackとretry時重複なしを検証。
- worker再構築時はShutdownをqueueへ送り、旧threadをjoinしてから新workerが履歴をseedする。
- DBの最大turn_idからTurnTrackerを再開し、再構築・再起動相当でturn_idが重複しないよう修正。
- 永続化失敗した完了turnはpendingに保持し、次のuser message受付時に再試行する。

## 2026-07-13 Phase 5 Task 3完了（ChatService 履歴永続化・復元）

- `ConversationOrchestrator::new_with_history` を追加し、DBから復元した直近履歴を設定上限内でpromptへseedする。
- ChatService worker起動時に `data/parallel-world.sqlite3` を開き、設定変更によるworker再構築時も履歴を再読込する。
- user/assistantの確定ペアだけをreply完了時に保存し、cancelled assistant断片と失敗turnは保存しない。
- SQLite open/read/write障害はwarningログ境界で一時履歴へ縮退し、会話workerを停止しない。
- `messages.id` はSQLite自動採番のまま、`turn_id` は相関情報としてのみ保存する。
- TDD RED: seed API、永続イベント境界、復元関数の未定義を確認。GREEN: application orchestrator 8件、desktop service 2件pass。

## 2026-07-13 Phase 5計画・Task 1完了（SQLite基盤）

### 内容

- `docs/superpowers/plans/2026-07-13-phase-5-history-memory.md` にPhase 5のTDD実装計画を作成。
- `pw-storage` crateを新設し、rusqlite 0.40.1のbundled SQLiteを採用。
- SQLite 3.51.3未満を起動時に拒否し、2026年に公表されたWAL-reset bug未修正版でWALを使用しないようにした。
- 接続ごとに外部キーを有効化し、busy timeoutを5秒に設定。ファイルDBはWAL、インメモリDBは通常journalを使用。
- migration `0001_initial.sql` で conversations / messages / conversation_summaries / memories と索引を作成し、`user_version=1` をトランザクション内で設定。

### TDD結果

- RED: `cargo test -p pw-storage` → `Database` / `StorageError` 未定義（期待どおり）。
- GREEN: 同コマンド → 2 passed / 0 failed（pragma/schema、WAL/reopen）。

## 2026-07-11 初期調査

### 現在の作業

- `基本設計.md`、`IDEA.md`、プロジェクト構成を確認中。
- サブエージェントで設計監査、リポジトリ監査、公式一次資料による最新ベストプラクティス調査を並行実施中。
- 実装前に完成スコープと受け入れ条件を確定する。

### 確認できた事実

- `apps/`、`crates/`、`packages/` などの想定ディレクトリは存在するが、製品コードはまだ存在しない。
- Live2D Cubism SDK公式サンプルと候補モデル素材が `third_party/`、`project-input/` に配置されている。
- 現在のフォルダはGitリポジトリではない。
- `基本設計.md` はPhase 0からPhase 7までの製品ロードマップと、最初に着手する縦方向の最小実装を別々に定義している。

### 次の判断

- 今回の完成スコープはPhase 0からPhase 7までの製品全体とする。
- 第21章はPhase 0からPhase 2に相当する先行縦スライスであるため、設計上の依存順に従い最初に完成させ、その後Phase 3からPhase 7へ進む。
- サブエージェントが実装を担当し、主エージェントが設計、差分レビュー、統合、テスト、最終確認を担当する。

## 2026-07-11 スコープ確定

### 決定

- 最終目標: `基本設計.md` のPhase 7までを実装する。
- 実装順: 第21章の縦スライス（Phase 0〜2）→ Phase 3 → Phase 4 → Phase 5 → Phase 6 → Phase 7。
- 各フェーズで、設計確認、TDD実装、サブエージェント成果物レビュー、テスト・ビルド、作業記録更新を行う。

### 制約

- サブエージェント起動APIにはモデル指定欄がないため、Lunaモデルを明示選択できない。利用可能なサブエージェントを使用する。
- Live2Dモデルの製品同梱・任意モデル追加はライセンス確認を完了条件に含める。
- コード署名、自動更新公開先、商用配布ライセンスなど外部の認証情報・契約が必要な項目は、実装とローカル検証を完了した上で、外部ゲートとして明示する。

### 設計承認

- 段階的モジュラーモノリスを採用する。
- 第21章の縦スライスからPhase 7までを依存順に実装する。
- 会話ターンIDとキャンセルトークンにより、古い応答の混入を防止する。
- 3ウィンドウの責務、IPC、Capability、CSP、秘密情報の境界を確定した。
- Phase 6とPhase 7の不足していた完成条件を補完した。
- 合意した設計を `docs/superpowers/specs/2026-07-11-parallel-world-product-design.md` に保存した。

## 2026-07-11 Phase 0計画

### 実行環境と固定候補

- Node.js 24.15.0
- npm 11.12.1
- ローカルpnpm 9.15.9（プロジェクトはpnpm 11.11.0へ固定予定）
- Rust/Cargo 1.96.0
- Tauri Rust crate 2.11.5、Tauri CLI 2.11.4
- React 19.2.7、Vite 8.1.4、Vitest 4.1.10、TypeScript 7.0.2

### 計画

- `docs/superpowers/plans/2026-07-11-phase-0-foundation.md` にPhase 0のTDD実装計画を作成した。
- Task 1: workspaceと会話状態domain。
- Task 2: Rust DTOからTypeScript契約を生成。
- Task 3: React/Viteの3画面shell。
- Task 4: Tauri 3ウィンドウ、command、Capability。
- Task 5: app data、ログ、CI、Phase 0受け入れ検証。

## 2026-07-11 Phase 0 Task 1完了

### 作成ファイル

- `Cargo.toml`（workspace: resolver 3、edition 2024、rust-version 1.96、`unsafe_code = "forbid"`、clippy all+pedantic warn）
- `rust-toolchain.toml`（1.96.0、rustfmt、clippy固定）
- `package.json`（private workspace、`packageManager: pnpm@11.11.0`）
- `pnpm-workspace.yaml`（apps/*、packages/*）
- `crates/pw-domain/`（`ConversationState` 14状態と `is_unavailable()`）
- `.gitignore` に `target/`、`node_modules/`、`dist/`、`.env`、`*.log`、`*.sqlite3`、`.superpowers/`、`.pnpm-store/` を追加

### TDD結果

- RED: `cargo test -p pw-domain` → E0432 `ConversationState` 未定義でcompile failure（期待どおり）。
- GREEN: `cargo test --workspace` → 2 passed / 0 failed。
- `cargo fmt --all --check` → exit 0。
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` → exit 0（pedantic doc-markdown指摘1件を修正済み）。

## 2026-07-11 Phase 0 Task 2完了

### 内容

- `crates/pw-contracts`: `SCHEMA_VERSION = 1`、`ConversationStateDto`（domainと同一variant、`From<ConversationState>` 実装）、`AppStatusDto { schema_version, conversation_state }`。
- ts-rs 12.0.1はAPIが変更されており、`export_all_to` ではなく `Config::new().with_out_dir(dir)` + `T::export_all(&config)` を使用（exporter: `cargo run -p pw-contracts --bin export-bindings`）。
- 生成物: `packages/contracts/src/generated/{AppStatusDto,ConversationStateDto}.ts`（手編集禁止）。
- `packages/contracts`: TypeScript 7.0.2、strict + `noUncheckedIndexedAccess` + `exactOptionalPropertyTypes` + `verbatimModuleSyntax`。`src/index.ts` は生成型の再exportのみ。
- pnpmはcorepack経由で `packageManager: pnpm@11.11.0` を使用（ローカル9.15.9は使わない。コマンドは `corepack pnpm ...`）。

### TDD結果

- RED: `cargo test -p pw-contracts` → E0432 DTO未定義（期待どおり）。
- GREEN: DTOテスト1件pass。bindings 2件生成。`corepack pnpm --filter @parallel-world/contracts typecheck` → exit 0。
- fmt / clippy(-D warnings) / `cargo test --workspace` すべてpass。

## 2026-07-11 Phase 0 Task 3完了

### 内容

- `apps/desktop`: Viteのmulti-page build（`character.html` / `chat.html` / `settings.html` をRollup inputへ指定）。各entryは `createRoot` + `StrictMode` で対応componentだけをmount。
- Character: 透過表示用に `body.transparent`、Live2D予約の `canvas`、`StatusBadge`（`role="status"`、初期状態は `starting` = 準備中）。
- Chat: `aria-live="polite"` の会話履歴領域、label「メッセージ」の入力、送信/停止ボタン。
- Settings: h1「設定」、nav 7項目（マイク/音声認識/LLM/音声合成/キャラクター/データ/診断）。
- `StatusBadge` は `Record<ConversationStateDto, string>` の全状態ラベル表を持ち、契約の状態追加時に型エラーで検出できる。
- 依存: React 19.2.7、Vite 8.1.4、Vitest 4.1.10、TypeScript 7.0.2、@vitejs/plugin-react 6、Testing Library、jsdom、@types/node。

### TDD結果

- RED: `corepack pnpm --filter @parallel-world/desktop test` → component未定義でresolve failure（期待どおり）。
- GREEN: 3 tests passed / 0 failed。
- typecheck: `tsc --noEmit` exit 0（`types` に node / vite/client / vitest/globals / jest-dom を追加して解決）。
- build: `dist/character.html`、`dist/chat.html`、`dist/settings.html` 生成。

## 2026-07-11 Phase 0 Task 4完了

### 内容

- `apps/desktop/src-tauri`: window labels `character` / `chat` / `settings` を `WINDOWS` 定数で宣言（Characterのみ transparent=true / decorations=false）。setupで不足windowだけを `WebviewWindowBuilder` から作成。
- 公開command: `get_app_status`（`AppStatusDto { schema_version: 1, conversation_state: idle }` を返す）のみ。`build.rs` の `AppManifest::commands(&["get_app_status"])` で公開集合をACLへ登録。
- Capability 3件（labelで対象window限定）: characterは `core:default` + `core:window:allow-start-dragging` のみ（shell / fs / 設定書込なし）、chatとsettingsは `core:default` + `allow-get-app-status`。
- CSP: `default-src 'self'; img-src 'self' asset: http://asset.localhost; style-src 'self' 'unsafe-inline'; script-src 'self'`（無効化しない）。
- `macOSPrivateApi: true`（透過window用）に伴い tauri crateへ `macos-private-api` featureを追加。
- アイコン: プレースホルダーPNGから `tauri icon` で生成（Phase 7で正式差し替え）。

### テスト

- window定義テスト2件（label一覧、透過/装飾の組み合わせ）。
- command単体テスト1件（schema_versionとidle）。
- Capability拒否テスト4件（`tests/capabilities.rs`: characterへのshell/fs/command漏れなし、chatはcustom permissionが `allow-get-app-status` のみ、等）。
- RED→GREEN確認済み。`cargo test -p parallel-world-desktop` 7件pass。
- `corepack pnpm --filter @parallel-world/desktop tauri build --debug --no-bundle` → exit 0、`target/debug/parallel-world-desktop.exe` 生成。

## 2026-07-11 Phase 0 Task 5完了（Phase 0基盤完成）

### 内容

- `crates/pw-platform`: `AppDataLayout::under(root)` が root + config/data/models/characters/voices/cache/logs/crashes/tmp の10ディレクトリを導出。`create_all()` と `all_directories()` を提供。
- `apps/desktop/src-tauri/src/bootstrap.rs`: app data dirからlayoutを構築・作成し、`tracing` の日次rotation file logger（`logs/parallel-world.log`、non-blocking、guardは `app.manage` で保持）を設定。API key・環境変数値はログへ出力しない。
- `apps/desktop/src-tauri/src/error.rs`: `AppError`（thiserror、秘密情報を含めない）。
- `.github/workflows/ci.yml`: windows-latest / macos-latest matrix、pnpm 11.11.0 + Node 24.15.0 + Rust 1.96.0、`pnpm install --frozen-lockfile` → typecheck / test / build → `cargo fmt --check` / `clippy -D warnings` / `cargo test --workspace`。
- `README.md`（製品概要、Phase進捗表、開発コマンド、外部ゲート）、`docs/development/getting-started.md`（前提、セットアップ、品質ゲート、契約再生成手順）。

### Phase 0 fresh verification（全件成功）

- `cargo fmt --all --check` → exit 0
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` → exit 0
- `cargo test --workspace` → 12 passed / 0 failed（domain 2、contracts 1、platform 2、desktop unit 3、capability 4）
- `corepack pnpm typecheck` → exit 0
- `corepack pnpm test` → 3 passed / 0 failed
- `corepack pnpm build` → 3 HTML entry生成
- `corepack pnpm --filter @parallel-world/desktop tauri build --debug --no-bundle` → exit 0、debug executable生成

### 残る外部ゲート（Phase 7で解消）

- Windows code-signing証明書、Apple Developer資格情報とnotarization
- updater公開URLと署名秘密鍵
- Live2D SDKリリース許諾、同梱モデル再配布許諾、各AIモデルのライセンス確認
- アプリアイコンはプレースホルダー（Phase 7で正式版に差し替え）

### 次: Phase 1（Live2D表示）

- `packages/live2d-runtime` にReact非依存のLive2D controllerを実装（Cubism SDK for Web + `project-input/live2d/` の検証済み素材を使用）。
- Character Windowの透過canvasへモデル表示、ドラッグ、位置・サイズ復元。
- 実装計画は着手時に `docs/superpowers/plans/` へ作成する。

## 2026-07-11 Phase 1 Task 1完了

### 決定と内容

- 実装計画を `docs/superpowers/plans/2026-07-11-phase-1-live2d.md` に作成（5 Task構成）。
- SDK vendor: Cubism Web Framework 5-r.5（TSソース+LICENSE）を `packages/live2d-runtime/vendor/framework/` へ、再配布許可済みCore（`.d.ts` は vendor/core、`.min.js` は `apps/desktop/public/live2d/core/`）、WebGLシェーダーを `public/live2d/shaders/` へ配置。出所は `vendor/README.md` に記録。
- `.gitignore`: 大文字小文字非区別により `**/Live2DCubismCore.*` が小文字のCore成果物もブロックするため、再配布許可2ファイルのみ negation で許可。開発用モデル置場 `public/live2d/models/` を除外。
- `@parallel-world/live2d-runtime`: `CubismRuntime` / `ModelHandle` インターフェースと `Live2DController`（idle→starting→ready→model-loaded / unavailable / disposed）。fake runtime注入で10テスト。
- vendor Frameworkはstrict非対応（strictNullChecks系654エラー）のため、このパッケージのみ `strict: false + noImplicitAny + useDefineForClassFields: false`。他パッケージのstrictを守るため **distビルド配布**（`tsc -p tsconfig.build.json`、exports は dist を指す）。

### TDD結果

- RED: controller未実装でresolve failure → GREEN: 10 tests passed。typecheck / build exit 0。

## 2026-07-11 Phase 1 Task 2完了

### 内容

- `CharacterModel`（`CubismUserModel` 継承、公式サンプルLAppModelのpromise版）: model3.json → moc3 / expressions / physics / pose / eyeblink / breath / look / UpdateScheduler / layout / motions事前読込 / renderer+シェーダー / テクスチャ（`createImageBitmap` premultiply）。
- `CubismFrameworkRuntime`（`CubismRuntime` 実装）: Coreグローバル検出（無ければstart reject→縮退）、WebGL2/1 context（透過+preserveDrawingBuffer）、rAF描画loop、サンプル準拠のprojection、alpha読み `hitTest`（クリック透過用）。
- 純粋ロジックをTDD: `modelBaseUrl`/`resolveResourceUrl`（URL解決・エンコード）、`FrameTimer`（clamp付きdelta）、`resolveIdleGroup`（Idle→大文字小文字不問→先頭グループfallback）。
- 注意: `_expressions` はサンプル側フィールドのためCubismUserModelには存在しない。自前Mapで管理。

### 結果

- 21 tests passed（4ファイル）。typecheck / dist build exit 0。WebGL実描画はT4のtauri devで確認する。

## 2026-07-11 Phase 1 Task 3完了

### 内容

- DTO追加: `CharacterManifestDto { schema_version, model_path, expressions, motion_groups }` / `MotionGroupDto`。bindings再生成、contracts typecheck pass。
- `character/manifest.rs`: model3.json解析（Expressions/Motionsを抽出、合成fixtureでTDD 5件）と `find_first_model3`（characters配下を再帰探索、ソート順）。
- commands追加: `get_character_manifest`（発見→解析→`CharacterState` Mutexへキャッシュ→DTO返却）、`set_expression` / `start_motion`（manifest照合で検証→characterウィンドウへ `character-expression` / `character-motion` eventをemit）、`set_click_through`（`set_ignore_cursor_events`）。
- `AppDataLayout` を `app.manage` で共有（bootstrapの戻り値を管理）。
- Capability: characterへ manifest+click-throughのみ追加（set-expression/start-motion漏れ禁止をテストで固定）、settingsへ character制御3件追加。拒否テスト更新（4件）。
- `tauri.conf.json`: assetProtocol有効化（scope `$APPDATA/characters/**`）+ CSP connect-srcへ `asset: http://asset.localhost` 追加（`protocol-asset` featureも追加）。
- `tauri-plugin-window-state` 2.4.1: plugin登録+setupで全ウィンドウ `restore_state`（位置・サイズ復元）。
- clippy注意: tauri commandは所有引数必須のため `needless_pass_by_value` を関数単位でallow。

### 結果

- fmt / clippy(-D warnings) exit 0。Rust 21 tests passed（desktop 11 + capability 4 + contracts 2 + domain 2 + platform 2）。

## 2026-07-12 Phase 1 Task 4完了

### 内容

- `CharacterWindow` 統合: Coreグローバル検出 → `@parallel-world/live2d-runtime/cubism` を動的import → controller attach → `get_character_manifest` → `convertFileSrc` でモデル読込 → `character-expression` / `character-motion` event購読。ResizeObserver+`devicePixelRatio` でDPI対応、canvasに `data-tauri-drag-region` でドラッグ移動。
- **重要な設計変更**: vendored FrameworkのJSはモジュール評価時に `Live2DCubismCore` グローバルを参照するため、アダプタをindexから外しサブパスexport `./cubism` に分離。Core未ロード環境ではimport自体を行わず `renderer_unavailable` へ縮退（テストで固定）。
- `character.html` に `/live2d/core/live2dcubismcore.min.js` のscriptタグ（CSP `script-src 'self'` 内）。
- Settings: `CharacterPanel`（manifest取得→表情select→`set_expression`、モーショングループ→`start_motion`、失敗時 `role="alert"`）。テスト4件。
- `tools/scripts/sync-live2d-dev-assets.mjs`: epsilon_free runtimeを `%APPDATA%/com.parallelworld.desktop/characters/` へコピー（モデルはコミット禁止のため）。
- CIは `pnpm build` を typecheck/test の前に移動（desktopがlive2d-runtimeのdistを参照するため）。getting-started更新。

## 2026-07-12 Phase 1 Task 5完了

### 内容

- 契約追加: `CharacterCursorEventDto { schema_version, x, y }`（CSS px、bindings再生成済み）。
- `windows/cursor.rs`: 50msごとにRustが `cursor_position()` をポーリングし、`relative_css_position`（純粋関数、テスト3件）でウィンドウ相対CSS座標へ変換して `character-cursor` eventをcharacterウィンドウへemit。クリック透過中もIPCは届くため復帰可能。
- `CharacterWindow`: cursor eventでモデルalpha hit-test（`preserveDrawingBuffer` + `readPixels`）し、変化時のみ `set_click_through` をinvoke（モデル上=操作可能、透過部=クリック透過）。モデル未読込時は透過しない。

### Phase 1 検証結果

- Rust: fmt / clippy(-D warnings) exit 0、25 tests passed（cursor 3 + cursor DTO 1追加）。
- Frontend: typecheck 3パッケージ exit 0、tests 28 passed（live2d-runtime 21 + desktop 7）。
- build: live2d-runtime dist + desktop 3 HTML entry。
- 実機確認（要ユーザー操作）: `node tools/scripts/sync-live2d-dev-assets.mjs` → `corepack pnpm --filter @parallel-world/desktop tauri dev` でepsilon表示・Settings画面から表情/モーション切替・再起動後の位置サイズ復元・クリック透過を確認する。

## 2026-07-12 実機確認トラブルシュート

- 症状: Settingsに「no character model found」。調査の結果、**AI支援環境のシェルはMSIXコンテナでAppData書き込みが仮想化**されており、AI側で実行したsync scriptのモデルは `AppData\Local\Packages\Claude_*\LocalCache\Roaming\...` に書かれていた（ユーザーのアプリからは不可視）。
- 対応: モデルsyncと `tauri dev` は**ユーザー自身のPowerShellで実行**する運用に確定。改善としてSettingsのキャラクター欄に「再読み込み」ボタンを追加、モデル読込失敗時はCharacter Windowを `renderer_unavailable` 縮退表示へ（コミット済み）。
- 補足: 途中で複数のdev server/アプリの残存プロセス（port 5173/5174）が混在したため全停止した。`tauri dev` は同時に1つだけ起動すること。
- 追加バグ修正: 相対リソースが `http://asset.localhost/Epsilon_free.moc3: 403` で失敗。Tauriのasset URLはファイルパス全体を1セグメントとしてencodeするため、URL空間でのディレクトリ解決は成立しない。`ModelSource`（modelUrl + resolveResource）を導入し、**パス空間で相対解決→リソース毎にconvertFileSrc**する方式へ変更（`createModelSource`、テスト追加）。

## 2026-07-12 Phase 1 実機確認結果

- ユーザー確認済み: キャラクター（epsilon_free）のデスクトップ表示、設定画面からの表情切替、モーション再生 → **正常動作**。
- 未確認（次回セッション冒頭で確認推奨）: 再起動後の位置・サイズ復元、クリック透過の実挙動。
- Phase 1の主要完了条件（安定表示・表情/モーション切替）を実機で満たした。

## 2026-07-12 Phase 2 Task 1〜3完了

### 計画とモデル取得承認

- 実装計画を `docs/superpowers/plans/2026-07-12-phase-2-audio-stt.md` に作成（6 Task構成）。
- ユーザー承認: silero_vad.onnx（約2MB、MIT）と ReazonSpeech zipformer ONNX（約600〜700MB、Apache-2.0）のダウンロード、sherpa-onnxネイティブビルドを「すべて許可」。

### Task 1: pw-domain speech（TDD、16テスト）

- `SpeechSegmenter`: VAD確率列→Started/Completed/DiscardedTooShort。pre-roll、min speech、hang、max segment、平均確率。10分無音で無イベントをテストで固定。
- `TranscriptFilter`: EmptyText / TooShortSpeech / LowVadConfidence / AcousticTagOnly（括弧タグ・記号除去後に空）/ CaptureDisabled。

### Task 2: pw-audio（cpal 0.18.1 + rtrb 0.3 + rubato 0.16）

- `list_input_devices`（cpal 0.18の `DeviceId`（Display/FromStr永続化）+ `description().name()`）。
- `start_capture`: cpal Streamは非Sendのため**専用スレッドが所有**。callbackは format変換+mono mixdown+rtrb push のみ（事前確保scratch、割り当てなし）。drop数はAtomicU64。
- `MonoResampler`（FastFixedIn、1024チャンク、同レートはpass-through）: 48k→16kで正弦波の周波数保存をゼロクロスで検証。
- `CaptureFrameSource`/`ResamplingFrameBuffer`: pw-applicationの `SpeechFrameSource` port実装（純粋部分を分離してテスト）。
- ハマりどころ: cpal 0.18は `windows >=0.61,<=0.62` を許すがcargoが windows 0.61 + windows-core 0.62 の不整合で解決しビルド不能 → `cargo update -p windows-core@0.62.2 --precise 0.61.2` で統一。rubato 4.0はaudioadapter前提のAPI刷新のため0.16を採用。

### Task 3: pw-application（TDD、6テスト）

- ports: `SpeechFrameSource` / `VoiceActivityDetector` / `SpeechRecognizer` / `SpeechEvents`（依存方向: adapter→application→domain を厳守、pw-applicationはpw-domainのみに依存）。
- `SpeechPipeline`: 同期frame駆動core（テスト容易性のためスレッドは `run_pipeline` に分離）。pre-roll履歴、mute時のセグメント破棄と復帰、診断カウンタ。
- 完了条件シミュレーション: **無音10分（18,750フレーム）で送信0件**、短文1回で1 transcript（pre-roll込みサンプル数を検証）、mute中0件、棄却がtranscriptに漏れないこと。

### 検証

- fmt / clippy(-D warnings) exit 0。Rust 53テストpass（+ hardware ignored 1）。

## 2026-07-12 Phase 2 Task 4完了（sherpa-onnx実アダプタ）

### spike結果（ADR: docs/adr/2026-07-12-sherpa-onnx-binding.md）

- **公式 `sherpa-onnx` 1.13.4 crate（safe wrapper + sys）を採用**。当方コードにunsafe不要で `unsafe_code = "forbid"` 維持。ネイティブライブラリは公式GitHub releasesのprebuiltをビルド時取得。
- sherpaのVADはセグメント指向で生確率を公開しないため、`SileroVad` アダプタは `detected()` を確率1.0/0.0に写像する**二値話者検出器**とし、セグメンテーションはdomainの `SpeechSegmenter` に一元化。将来必要なら `ort` 直接推論へ差し替え可能（port契約不変）。

### モデル取得（承認済み）

- Silero VAD v5.1.2（MIT、2.3MB）: SHA-256 `2623a295...bdd788f`。
- ReazonSpeech k2 v2 sherpa変換版（Apache-2.0、713MB）: SHA-256 `e0981d0d...b5842d`。
- manifest（URL/SHA-256/license/配置先）を `content/model-manifests/{vad,stt}/` に作成。ローカルキャッシュは `.models-dev/`（gitignore）。ユーザー環境への配置は `node tools/scripts/download-stt-models.mjs`（ユーザー自身のシェルで実行、SHA-256検証付き）。

### 実モデル結合テスト

- `PW_STT_MODEL_DIR=... cargo test -p pw-stt-sherpa -- --ignored` → **同梱日本語サンプル（test_wavs/3.wav）の認識に成功**（「ヤンバルクイナ」を含む正しいテキスト、3.3秒）。
- 学び: transducerは無音入力でもトークンを幻出し得る（無音→空文字のテスト前提は誤り）。VAD+棄却フィルターの必要性の実証。

### 検証

- fmt / clippy(-D warnings) exit 0。workspace 53テストpass（ignored: hardware 1 + 実モデル1）。

## 2026-07-12 Phase 2 Task 5完了（Tauri統合と診断UI）

### 内容

- DTO追加: `AudioDeviceDto` / `AudioDiagnosticsDto` / `SttPhaseDto` / `SttStateEventDto` / `TranscriptEventDto` / `AudioLevelEventDto`（bindings再生成）。
- `speech/service.rs`: `SpeechService`（Mutexで単一パイプライン管理）。`start_listening` はworkerスレッドでモデルロード→capture→pipeline実行し、進捗を `stt-state` event（starting/listening/stopped/unavailable）で通知。モデル未配置は `unavailable` + 案内メッセージへ縮退。`stop_listening` / `set_capture_enabled`（ミュート、Phase 4でTTS再生ガードに使用）/ `get_audio_diagnostics`。
- events: `stt-transcript`（chat+settings）、`stt-level`（settings、8フレーム毎に間引き）、`stt-state`（chat+settings）。
- Capability: 音声5 commandsは**settingsのみ**。character/chatへの漏れ禁止をテストで固定（capability 5テスト）。
- Settings `MicrophonePanel`: デバイス選択（既定を自動選択）、開始/停止、ミュート、`<meter>`レベル表示、診断カウンタ（listening中2秒毎ポーリング）。テスト4件。
- `ChatWindow`: `stt-transcript` 購読→会話履歴（`aria-live`）へ追記。テスト1件。

### 検証

- Rust 58テスト + frontend 32テスト、typecheck / build / clippy / fmt すべてpass。
- `tauri build --debug --no-bundle` exit 0（sherpa-onnxネイティブリンク込み）。

## 2026-07-12 Phase 2 Task 6完了（受け入れ検証）

### 実モデルE2E受け入れテスト（crates/pw-stt-sherpa/tests/e2e_pipeline.rs、#[ignore]、要モデル）

- **無音10分でLLM送信0件**: 実Silero VAD + 実ReazonSpeechのフルパイプラインに10分相当（18,750フレーム）の無音を投入 → transcript 0件。**PASS**
- **通常の短文を安定して認識**: 同梱日本語音声をフルパイプライン（VAD→segmenter→STT→filter）へ投入 → 正しく1件認識。**PASS**（2テスト6.3秒）
- 実行方法はgetting-startedに記載（PW_VAD_MODEL / PW_STT_MODEL_DIR）。
- 注意: 外部testクレートで `impl SpeechEvents for Arc<T>` はorphan rule違反 → newtypeでラップ。

### Phase 2完了条件の状態

- 無音10分で送信0件: 実モデルE2EでPASS。
- 短文の安定認識: 実モデルE2EでPASS（実マイクでの確認はユーザー実機確認項目）。
- TTS再生中のSTT停止: `set_capture_enabled` 実装済み・パイプラインテストでmute挙動を検証済み（TTSはPhase 4で接続）。
- 2時間メモリ非増加: 診断カウンタ（drop数等）を実装済み。長時間試験はPhase 6の長時間安定性検証で実施（基本設計のPhase 6項目と統合）。

### 実機確認手順（ユーザー、自身のPowerShellで）

1. `node tools/scripts/download-stt-models.mjs`（初回のみ、.models-devキャッシュがあるため再ダウンロードなし）
2. `corepack pnpm --filter @parallel-world/desktop tauri dev`
3. 設定「マイク」でデバイス選択→聞き取り開始→話す→チャットウィンドウに認識結果が出ること、レベルメーター動作、ミュートで認識が止まることを確認。

## 2026-07-12 Phase 2 実機確認とイベント二重配信の修正

- ユーザー実機確認: マイク聞き取り・認識・レベルメーター・診断カウンタは**正常動作**。ただし認識結果がチャットに2重表示（診断は採用3、表示6件）。
- 原因: Tauri 2の `emit_to(label)`（`EventTarget::Labeled`）は同一ラベルの Window と WebviewWindow の**両ターゲットに一致し二重配信**される。
- 修正: 全emit箇所（stt-transcript / stt-state / stt-level / character-expression / character-motion / character-cursor）を `EventTarget::webview_window(label)` へ変更。今後のイベント追加でも `emit_to(&str)` は使わないこと。
- 上記修正後も2重表示が継続（採用6に対し表示12）。tauri本体とJS APIのソース精査の結果、配信機構は「リスナー1つ=1回呼び出し」で健全。真因はフロント側: `listen()` は登録・解除とも非同期のため、React StrictModeの効果二重実行と競合すると解除が漏れて購読が2本残る。
- 恒久修正: `src/shared/ipc/event-bus.ts` を導入。ウィンドウ毎にイベント名につきTauriリスナーを1本だけ保持し、コンポーネントはSetへの**同期的**なhandler登録/解除のみ行う（StrictMode耐性が構造的に保証される）。ChatWindow / MicrophonePanel / CharacterWindowを移行。今後の`listen()`直接使用は禁止、必ず`subscribeEvent`を使う。
- **真因確定（bus導入後も採用1に対し表示2で継続）**: JSの `listen()` は既定でターゲット `Any` として登録され、**どのウィンドウ宛のemitでも受信する**。バックエンドが同名イベントをchat宛+settings宛に**2回emit**していたため、chatの単一リスナーに2回届いていた。
- 最終修正（両側）: ①ブロードキャスト系（stt-transcript / stt-state）は `app.emit()` で**1回だけ**送信。②event-busのリスナーを `getCurrentWebviewWindow().listen`（自ウィンドウ限定ターゲット）に変更し、他ウィンドウ宛emitを構造的に受信しない。教訓: **同名イベントをウィンドウ別に複数回emitしてはならない**（Anyリスナーが全部受け取る）。
- ユーザー実機確認で2重表示の解消を確認。**Phase 2完了**。

## 2026-07-12 Phase 3完了（LLM会話）

### 計画

- `docs/superpowers/plans/2026-07-12-phase-3-llm-conversation.md`（5 Task構成）。

### T1: pw-domain reply（TDD 16テスト追加）

- `ReplyParser`: 1行目 `{...}` のみ制御JSON候補として改行までバッファ、それ以外は即Speechでストリーム（“改行が来ないと全文バッファ”の初期実装はキャンセル試験で発覚し修正）。
- `SentenceSplitter`: 逐次分割（。！？!?…・改行）。連続終端記号（！？）は同一文に保持するフラグ方式。
- `TurnTracker`: 単調増加turn_id、現在判定、invalidate。

### T2: ConversationOrchestrator（TDD 5テスト）

- 状態遷移の唯一の所有者。Idle→Thinking→Speaking→Idle、cancelで残デルタ・残文を抑止（emit毎にcancel検査）、LLM障害でLlmUnavailable+履歴維持+`recover()`。履歴はcapped deque、プロンプトは 規則→キャラ設定→履歴→現在発話。全イベントにturn_id付与。

### T3: pw-llm（契約テスト4 + 単体4）

- 公式`reqwest(blocking)`+SSE解析（`data:`行、[DONE]、不正チャンクskip）。行間でcancel検査。
- loopback検証（`validate_base_url`、IPv6 `[::1]` はurl::Hostで判定）。remote は `allow_remote` 明示時のみ。
- tiny_httpモックで リクエスト形状 / delta結合 / 途中cancel / 4xx5xx / 不正SSE を固定。

### T4/T5: Tauri統合とUI

- DTO: `ChatMessageEventDto` / `ChatRoleDto` / `ConversationStateEventDto` / `LlmSettingsDto`。
- `ChatService`: mpsc queueの単一worker（設定fingerprint変更で再構築）。events: `chat-message` / `conversation-state`（app.emitで1回だけ）。制御JSONの emotion/motion は `character-expression` / `character-motion` へ写像し、**キャラクターの表情/モーション名一覧をキャラプロンプトへ自動注入**。
- STT接続: on_transcript → ChatService.submit（音声→応答）。
- `config/llm.json` 永続化（保存時にendpoint検証、TDD 3テスト）。commands: `send_chat_message` / `cancel_turn` / `get_llm_settings` / `set_llm_settings`。Capability: chatへsend/cancel、settingsへLLM設定（拒否テスト更新）。
- ChatWindow: 送信/Enter送信/停止の実配線、user/assistantメッセージ表示（turn_id付き）、状態表示（考え中…等）。Settings LLMパネル（接続先/モデル/リモート許可/キャラ設定/保存）。frontendテスト6件追加。

### 検証

- Rust 93テスト + frontend 37テスト、fmt/clippy(-D warnings)/typecheck/build/tauri build すべてpass。
- 完了条件: テキスト→応答（orchestrator+契約テストで担保、実機はOpenAI互換サーバー必要）、音声→応答（STT接続済み）、途中停止（cancelテスト済み）、古い応答の混入なし（turn_id+直列worker+cancelの3重防御）。実LLMサーバーでの確認はユーザー実機確認項目。

### 実LLMサーバー結合確認（2026-07-12）

- LM Studio（`http://127.0.0.1:1234/v1`、`google/gemma-4-12b`）に対し `crates/pw-llm/tests/real_server.rs`（#[ignore]）で orchestrator フルパスを検証。
- 結果: **制御JSON抽出成功**（emotion: Smile / intensity: 0.8 / motion: Idle — モデルがプロンプトの形式指示に追従）、**文章分割2文**、状態Idle復帰。24秒/turn（12B）。
- 実行: `PW_LLM_MODEL=<model> cargo test -p pw-llm --test real_server -- --ignored --nocapture`
- 注意: ローカル8080は別サービス（python.exe）が使用中のため、LM Studioは1234のまま使用。アプリ側はSettings LLMパネルで接続先を `http://127.0.0.1:1234/v1` に設定する。
- 実機トラブルシュート: 「LLMに接続できません」の原因は**設定未保存**（llm.json未作成→既定8080の別サービスに接続）。保存ボタン必須。エラー詳細が状態イベントで消えるUI問題も修正。
- ユーザー実機確認: LM Studio経由でチャット応答成功。**Phase 3完了**。

## 2026-07-12 絵文字制御（Phase 3追補）

- 要望: モデルによっては応答に絵文字が混入する。
- 対応（2段構え）: ①既定システムプロンプトに「絵文字・顔文字・記号の羅列は使わない」を追加（モデル依存）。②**決定的フィルター** `pw_domain::reply::strip_emoji`（絵文字・ZWJ列・肌色修飾・variation selector・dingbats・emoji化される矢印/図形を除去、和文記号は保持）をorchestratorの発話経路に適用。Phase 4のTTS読み上げ正規化の基盤を兼ねる。
- 設定: `LlmSettingsDto.strip_emoji`（serde defaultでtrue、既存llm.jsonとの後方互換あり）。Settings LLMパネルに「応答から絵文字を除去する」チェックボックス。
- テスト: domain 4件 + orchestrator E2E 1件（😊🎉👍🏻入りチャンク→除去済み文）。既存プロンプト保存済みユーザーは、プロンプト側の抑制文言は自動では入らない（フィルターは設定既定でON）。

## 2026-07-13 Phase 4 Task 1完了（pw-tts AivisSpeechクライアント）

### 内容

- 計画: `docs/superpowers/plans/2026-07-13-phase-4-tts-lipsync.md`（6 Task構成）。
- `validate_base_url`（loopback検証）を `pw-llm` から `pw-platform::net` へ移動（pw-llmは再エクスポートで互換維持、pw-ttsと共用）。
- `pw-tts` crate新設: `AivisSpeechClient`（VOICEVOX互換API、既定 `http://127.0.0.1:10101`、loopback限定）。
  - `speakers()` = GET /speakers、`synthesize()` = POST /audio_query → volumeScale/speedScale適用 → POST /synthesis（WAVバイト列、RIFFヘッダ検証）。
  - ユーザー辞書: `user_dict()` / `add_user_dict_word()` / `delete_user_dict_word()`。
  - `TtsError`: InvalidEndpoint / Transport / Api{status,detail} / Protocol。

### 検証

- tiny_http mock契約テスト6件（speakers解析 / audio_query→synthesisの形状とスケール適用 / 422→Api / 非WAV→Protocol / 辞書CRUD / 接続不能→Transport）+ 単体2件。
- workspace全テストpass、fmt / clippy(-D warnings) pass。

## 2026-07-13 Phase 4 Task 2完了（WAVキャッシュと合成キュー）

### 内容

- `pw-domain::reply::is_speakable`: 記号・空白のみの文をTTS投入前に除外（英数字・かな・漢字を1文字以上含む場合のみ合成）。
- `pw-tts::WavCache`: `cache_key`（FNV-1a 64、text|style|volume|speedから決定的生成）、lookup/store、上限（既定200）超過で更新日時の古い順に剪定（直近書き込みは常に保護）。
- `pw-application::speech_synthesis`: port `TtsSynthesizer`（文→WAVパス）/ `SpeechAudioSink`（on_audio(turn,seq,path,text) / on_stop / on_error）。
- `SpeechSynthesisQueue`（TDD 5テスト）: 到着順直列合成、turn毎にseq採番、新turnでseqリセット、旧turnの遅延文は破棄、`stop()` で現行turn失効+即時停止通知、合成失敗はturnにつき1回だけ通知し残りをスキップ（テキスト表示は継続、次turnで再挑戦）。

### 検証

- Rustテスト: pw-domain 37 / pw-application 19（+5）/ pw-tts 8（cache 3含む）すべてpass。fmt / clippy(-D warnings) pass。

## 2026-07-13 Phase 4 Task 3完了（contracts DTOとTauri統合）

### 内容

- DTO追加: `TtsSettingsDto`（enabled/base_url/style_id/volume/speed）/ `TtsSpeakerDto`（話者×スタイルをドロップダウン用にflatten）/ `SpeechAudioEventDto`（turn_id/seq/wav_path/text）/ `SpeechStopEventDto` / `TtsStateEventDto`（縮退通知）/ `UserDictWordDto`。bindings再生成。
- **turn_idのTS型を `number` に修正**（`#[ts(type = "number")]`）: ts-rs既定のbigintはJSONペイロードの実行時型(number)と不一致で、HEAD時点でtypecheckが失敗していた（既存バグ）。
- `pw-tts::CachedSpeechSynthesizer`: `TtsSynthesizer` port実装（キャッシュ照合→合成→保存）。
- `TtsService`（mpsc単一worker、設定fingerprintで再構築）: `enqueue(turn, text)`（enabled=false時はno-op、エンジン失敗は `tts-state` で縮退通知しテキスト表示継続）。**停止の即時性**: `stop()` は呼び出しスレッドで失効watermark（AtomicU64、enqueue済み最新turn）を上げて `speech-stop` を即emit。workerは合成前にwatermark以下のturnを破棄（キュー待ちのHTTP合成を跨いで待たない）。
- イベント: `speech-audio` / `speech-stop` は characterウィンドウ単一宛（`EventTarget::webview_window`）、`tts-state` は `app.emit()` 1回。
- 接続: ChatService `on_sentence` → `TtsService::enqueue`（文章先読み）。`cancel_turn` コマンドはLLMキャンセル+TTS停止を同時実行。
- `config/tts.json` 永続化（保存時loopback検証、既定: `http://127.0.0.1:10101`、Anneliノーマル 888753760、TDD 3テスト）。
- commands: `get_tts_settings` / `set_tts_settings` / `list_tts_speakers` / `list_user_dict` / `add_user_dict_word` / `delete_user_dict_word`（settingsのみ）、`set_speech_playback`（characterのみ、`SpeechService::set_capture_enabled(!active)` へ接続 = TTS再生中のSTT停止）。build.rsのcommand一覧へ追加。
- Capability更新+拒否テスト更新。asset scopeへ `$APPDATA/cache/tts/**` 追加（WAVはconvertFileSrcで取得）。

### 検証

- workspace全テスト + frontend 37テスト + typecheck / fmt / clippy(-D warnings) pass。

## 2026-07-13 Phase 4 Task 4完了（live2d-runtime リップシンクと音声プレイヤー）

### 内容

- `ExternalLipSyncProvider`（IParameterProvider実装、0..1へクランプ、NaN→0）を新設し、`CharacterModel` に vendored `CubismLipSyncUpdater` を配線。モデルの LipSync グループ未定義時は `ParamMouthOpenY` へフォールバック。`setLipSyncValue(v)` を `ModelHandle` / `Live2DController` に公開。
- `SpeechAudioPlayer`（TDD 8テスト）: `AudioSink` 抽象越しの順次再生キュー。同一turnは到着順、新しいturnは即割り込み（現行停止+キュー破棄）、古いturnの遅延itemは破棄。`onActiveChange`（STTゲート用）/ `onLevel`（リップシンク用、停止時0）。stale再生セッションのlevel/endedコールバックはセッション比較で無視。
- `WebAudioSink`: fetch→decodeAudioData→BufferSource+Gain+Analyser。フレーム毎に時間領域RMS→平滑化(0.5)→×4ゲインでmouth値。stop即時、fetch/デコード失敗は onEnded 扱いで次項目へ（縮退）。

### 検証

- live2d-runtime 27テスト（+10）/ frontend全47テスト / typecheck / build pass。

## 2026-07-13 Phase 4 Task 5完了（characterウィンドウ再生配線とSettings TTSパネル）

### 内容

- CharacterWindow: `speech-audio` 購読 → `SpeechAudioPlayer`（WebAudioSink、WAVは `convertFileSrc` で取得）→ `onLevel` を `Live2DController.setLipSyncValue` へ（音声と口の同期）。`onActiveChange` → `set_speech_playback`（再生中はSTTミュート）。`speech-stop` で即時停止+口を閉じる。unmount時にplayer破棄。
- ChatWindow: `tts-state`（available=false）で「音声合成に接続できません: 詳細」をalert表示（会話はテキストで継続）。テスト1件追加。
- Settings に `TtsPanel` 追加: 読み上げ有効化 / 接続先 / 話者スタイル（「話者一覧を取得」で `/speakers` から取得、未取得時は現在値を表示）/ 音量・話速スライダー / 保存。ユーザー辞書: 読み込み・一覧・追加（単語/読み/アクセント核）・削除。テスト4件。

### 検証

- frontend 52テスト（+5）/ typecheck / build pass。

## 2026-07-13 Phase 4 Task 6完了（受け入れ検証）

### 実エンジンE2E（crates/pw-tts/tests/real_engine.rs、#[ignore]、要AivisSpeech Engine）

- `/speakers` 取得 → 先頭スタイルで合成 → RIFFヘッダとサンプルデータの存在を検証。
- `CachedSpeechSynthesizer` の2回目呼び出しがキャッシュhit（同一パス・mtime不変）であることを検証。
- 実行方法はgetting-startedに記載（`PW_TTS_BASE_URL` で接続先変更可）。

### Phase 4完了条件の状態

- 文章単位で順番に読み上げ: 合成キュー（seq採番・直列worker）+ SpeechAudioPlayer（順次再生）のテストで担保。実機はエンジン起動後に確認。
- 音声開始と口の動きの同期: WebAudioSinkのRMS→setLipSyncValue→CubismLipSyncUpdater配線。実機確認項目。
- 停止操作で即座に停止: cancel_turn → LLMキャンセル + TTS失効watermark + speech-stop（即emit）→ player.stop()。ユニットテスト済み。

### 品質ゲート

- cargo fmt/clippy(-D warnings)/test（workspace、Rust 117テスト）、pnpm typecheck/test（52）/build、tauri build --debug --no-bundle すべてpass。

### 実機確認手順（ユーザー、自身のPowerShellで）

1. AivisSpeech Engine（またはAivisSpeechアプリ）を起動（既定ポート10101）。
2. `corepack pnpm --filter @parallel-world/desktop tauri dev`
3. 設定「音声合成」で「話者一覧を取得」→ スタイル選択 → 保存（**保存を忘れない**、LLM設定と同様）。
4. チャットへ話しかける/入力 → 文章単位で読み上げ・口が動くこと、停止ボタンで音声と口が即止まること、読み上げ中にマイク認識が走らないことを確認。
5. エンジン未起動時: チャットに「音声合成に接続できません」が出て、テキスト応答は継続すること。

## 2026-07-13 Phase 4 実機確認完了

- AivisSpeech Engine未起動による「音声合成に接続できません」を診断（10101未listen、エンジン実体は `%LOCALAPPDATA%\Programs\AivisSpeech\AivisSpeech-Engine\run.exe` で dev-up.ps1 の検出パスに追加）。
- エンジン起動後、ユーザー実機で音声再生を確認。**Phase 4完了**。
- 引き継ぎ資料を `docs/development/handoff-2026-07-13.md` に更新（Phase 0〜4完了、次はPhase 5 履歴と記憶）。
## 2026-07-13 Phase 5 Task 2完了（会話履歴repository）

### 内容

- `pw-application::history` に `ConversationHistory` port、`StoredConversation`、`StoredMessage`、`MessageRole` を追加。
- `pw-storage::SqliteConversationHistory` にconversation upsert、message追加、conversation一覧、時系列message取得、transactionによるconversation削除を実装。
- 実SQLite DBでconversationの更新順、同一時刻messageの安定順序、role、turn_id、ファイルDB再open後の読出し、外部キーcascade削除を検証。
- TDD: API未定義による対象テストのREDを確認後、最小実装で2テストGREENを確認。

### 検証

- `cargo test -p pw-storage history`: 2 passed。
- application/storage全テスト、fmt、clippyはTask完了時の品質ゲートで確認。

### レビュー修正

- DB読出し時の未知roleと負のturn_idを黙って変換せず、明示的なadapter errorとして返すよう修正。
- cascade削除後にDBを再openし、削除対象のmessageが消え、対象外conversation/messageが保持されることを実DBで検証。
- conversation upsertの`updated_at`を`MAX`で単調増加にし、古い時刻の再送で後退しないテストを追加。

## 2026-07-13 Phase 5 Task 4完了（要約・長期記憶・prompt注入）

### 内容

- `pw-application::memory` に `MemoryStore` port、件数・文字数上限付き `MemoryContext`、古い確定範囲だけを処理する `SummaryWorker` / `SummaryGenerator` 境界を追加。
- SQLite schema version 5として外部コンテンツFTS5（trigram tokenizer）と同期triggerを追加し、summaryとmemoryの作成・読出し・更新・削除・関連度検索を実装。
- prompt順序を「system rules→character→user settings→関連長期記憶→会話要約→recent history→current utterance」に固定し、空sectionを省略。
- ChatService workerが発話ごとにmemoryとsummaryを再読込して注入。検索・要約読込失敗は警告ログ後に空contextで会話を継続。
- TDD RED: `MemoryContext`、`MemoryStore`、`build_with_context` 未定義によるcompile failureを確認。GREEN: prompt順序、空section、FTS再open/update/delete、要約の古いwindow、検索失敗継続を検証。

### 検証

- `cargo test -p pw-application`
- `cargo test -p pw-storage`
- `cargo test -p parallel-world-desktop chat::service`
- `cargo fmt --all --check`
- `cargo clippy -p pw-application -p pw-storage -p parallel-world-desktop --all-targets -- -D warnings`

### レビュー修正

- context検索を専用 `pw-memory-context` thread/queueへ移し、遅いSQLite検索がsubmit senderと会話workerを停止しない構成へ変更。
- 確定ターン通知を専用 `pw-memory-enrichment` threadへ実配線し、直近4件を除く古い安定範囲の要約と、明示的な持続的ユーザー事実の抽出・upsertを通常会話と分離して実行。
- enrichment生成物がファイルDBへ残り、再open後のcontextへsummary/memoryとして注入される統合テストを追加。
- memory DB open/migration失敗時はin-memory空contextへ縮退し、検索・生成失敗は警告のみで会話を継続。
- v4→v5 migrationで既存memoryをFTSへbackfill。日本語1〜2文字検索は `%` / `_` / `\\` をescapeしたLIKE fallbackを使用。

### 再レビュー修正

- storage永続化直前に秘密情報classifierを適用し、API key/token/password/secret/Authorizationラベルと高entropy credential形のmemory/summaryを拒否。
- 持続事実抽出を明示的一人称の肯定文に限定し、質問・否定・他者・仮定・引用を除外。
- `through_message_id` より後かつ直近4件より前の未要約範囲を最大8件ずつ処理し、既存summaryへrolling merge。DB保存summaryを2,000文字以内に固定。
- enrichmentはjobごとにDBを再openし、一時的なopen/migration/job失敗をwarning後に次jobで回復。queue送信失敗もwarningへ記録。
- schema version 6でmemoryのsource/content unique indexを追加し、migration内で既存重複を整理して競合時の重複を防止。

### 最終レビュー修正

- `update_memory` を含むcontent書込み全経路へ同一secret filterを適用し、日本語のAPIキー・トークン・パスワード・秘密・認証・ベアラーも短い値から拒否。
- 質問記号と疑問詞は文分割前にmessage全体で判定し、質問に併記された事実を保存しない。
- unsafe-only summary batchでも空contentと `through_message_id` を保存してcursorを進め、後続safe batchが停滞せず処理されるテストを追加。
- 個別factの拒否はwarning/skipとしてsummary進行を継続。
- v6 dedupeをunique indexと同じ `IFNULL(source_conversation_id, '')` 単位に合わせ、NULL/空source混在upgradeを検証。
## 2026-07-13 Phase 5 Task 5完了（履歴表示・データ管理）

- `ConversationMessageDto` と永続message IDを含む型付き履歴IPCを追加し、TypeScript bindingsを再生成。
- Chat起動時にSQLite履歴を読み込み、初期ロードとlive eventの競合を永続ID／turn-role-contentキーで重複排除。SQLite障害時は空履歴で継続。
- Settings専用のデータパネルを追加。明示保存先へのOnline Backup APIによる整合スナップショット、確認必須の会話履歴／要約・長期記憶削除を実装。
- 削除前にChat/context/enrichment workerを停止し、次の送信で削除後DBから再構築することで古いprompt状態の復帰を防止。
- RED: backup API未実装、DataPanel未実装、初期履歴未ロード、capability不足を対象テストで確認。GREEN: frontend 29件、capability 5件、workspace Rust tests、typecheck、fmt、clippyを検証。
- Importantレビュー修正: 本文dedupを廃止してlistener先行登録＋turn/role high-waterへ変更。同文別turnとassistant文結合、履歴raceをテスト。delete/submit共通排他、export同一DB・ディレクトリ・既存ファイル検証と明示overwrite、履歴ロード警告、DataPanel busy/errorを追加。
- 残Importantレビュー修正: export先をcanonicalizeし、`same-file`によるWindows hardlink/file identity判定を追加。既存symlink/junction/非通常ファイルを拒否し、新規先はcanonical parentとfilenameから構築。shutdown失敗時もcancelを必ず復旧し、DataPanelはbackendの既存ファイル応答後だけ上書き確認する二段階処理へ変更。
## 2026-07-13 Phase 5 Task 6完了（受け入れ検証・文書化）

- 同一app-data相当で保存、DB再open、履歴取得、summary/memory prompt注入、削除後の空状態、次promptへの非混入、Online Backup snapshot再openを一連の受け入れテストで検証。
- schema v1〜v6 upgrade、FTS backfill、memory deduplicateのmigrationテストを含む品質ゲートを実行。
- getting-started、handoff、README進捗をPhase 5完了へ更新。
- 最終レビュー: 永続化前のcredentialを決定的に`[REDACTED]`化し、future schema拒否、履歴削除時の要約cascade契約、型付き削除イベントによるChat表示clearを追加。上書きキャンセルは正常no-opの二段階確認を維持。
## 2026-07-13 Phase 6 Task 1完了（runtime health・障害分類・backoff policy）

- 計画: `docs/superpowers/plans/2026-07-13-phase-6-stability-recovery.md` のGlobal Constraints / Task 1に準拠。
- `pw-domain::runtime_health`: `RuntimeFeature`、`HealthStatus`、typed `FailureClass`、明示的な`Stopped`遷移を追加。`last_error`は共通診断redactionを通し、credential値を保持しない。
- `pw-application::recovery::BackoffPolicy`: clock/RNG注入、base 250ms、cap 30秒、full jitter、healthy 60秒でreset、連続8失敗でcircuit open。
- `pw-contracts`: schema_version付き`RuntimeHealthEventDto`、関連enum、イベント名`runtime-health`を追加し、TypeScript bindingsを再生成・公開。

## 2026-07-13 Phase 6 Task 2完了（外部process supervisor）

- `pw-platform::process::ProcessSupervisor`: Child/PID/generationを単一所有し、stdout/stderrを上限付きで常時drain、終了検知、generation照合、明示stop後の猶予待機→kill→waitを実装。Drop時も同じ回収経路を通しorphanを防止。
- helper child統合テストで即死、遅延終了、stderr flood、hang、spawn失敗、stop race、stale generation、process healthをRED→GREENで検証。retry cap/full jitter/circuitはTask 1の`BackoffPolicy`を再利用。
- desktop AppStateに`ManagedProcesses`を追加。`PW_AIVIS_ENGINE`または`PW_LLAMA_SERVER`で実行ファイルが明示された場合だけ所有・監視・再起動し、URL接続だけの既存外部serverは起動・停止対象にしない。
- レビュー修正: `command-group` 5.0.1のsafe wrapperでWindows Job Object / Unix process groupを所有し、tree killを保証。executableはabsolute/canonical/regular fileに限定し、child環境は`env_clear`後にOS必須変数と機能別envだけを渡す。引数はquoted pathを壊さない`PW_AIVIS_ARGS_JSON` / `PW_LLAMA_ARGS_JSON`配列へ変更。
- loopback TCP readiness probe（AivisSpeech 10101、llama-server 8080、環境変数port override）を追加。起動前にprobe成功なら既存外部serverとして非所有、owned childが15秒以内にlistenしなければ障害としてgraceful request（stdin close）→5秒→process tree kill→wait。drain threadもDrop時にjoinする。
- Tauri `RunEvent::ExitRequested` / `Exit`から`ManagedProcesses::shutdown`を明示呼び出しし、全monitor/child回収完了まで待機する。
- Importantレビュー追補: loopback TCP mockによるHTTP 2xx/503とfallback pathのprobe試験、`ManagedProcesses::restart`による明示停止・再arm、monitor generation照合によるstale loop停止を追加。
- Windows helperが子孫processを生成するtree-kill試験を追加し、停止後に`tasklist`でorphanが残らないことを検証。Settings診断に`runtime-health` DTO/eventを購読する最小UI（owned/external、retry回数、circuit open）を追加。
- TDD RED: domain/application/contractsの未定義APIによるcompile failureを確認。GREEN: 対象テスト6件pass。
- レビュー修正: Phase 5の永続化redactionをdomain共通実装へ移し、Authorization/JSON/query/key-value/quoted/日本語credentialと長さ上限を検証。failure APIをtyped code/detailへ変更してraw causeを非保持化。Healthy反復probeの時刻固定、Cancelled除外、悪性RNG clamp、`live2d` wire名、domain→DTO変換を追加。
- 再レビュー修正: credential置換coreと診断上限を分離。永続化は長文・一般文との混在を含め全文scanし、非秘密部分を切り詰めない。RuntimeFailureはenum codeと任意の数値system codeのみ受け付け、last_errorを固定安全文に限定。
## 2026-07-13 Phase 6 Task 4完了（STT再初期化とSpeechService supervisor）

- `SpeechService` がworker `JoinHandle`を所有し、明示停止時にcancel後joinするよう変更。
- CPALのbounded failure channelをSpeech pipelineへ接続。切断時はcapture/pipeline/drop mirrorを停止・joinしてからセッション全体を再構築する。
- Audio / VAD model / STT model / VAD runtime / STT runtime / explicit stopを型付き分類。モデル欠落は即circuit、一時障害は250ms〜30秒・最大8回で再試行する。
- 再構築中も同じ`capture_enabled`を共有し、TTS再生中のmute状態を保持する。
- STT/Audio Inputの`runtime-health` eventをhealthy/recovering/degraded/stoppedで発行する。
- TDD: retry上限、恒久モデル欠落、明示停止非retryをRED確認後に実装。desktop 58 unit tests + capability 6 tests成功。

## 2026-07-13 Phase 6 Task 8（最終受け入れ）

- STT初期化/runtime、音声device切断、LLM停止/hung、TTS停止、Live2D boot、owned process crash、SQLite縮退、crash secret absenceをproduction service/supervisor/recovery core/commandの自動テストへ対応付けた障害マトリクスを `docs/development/phase6-acceptance.md` に追加。
- `tools/scripts/soak-test.ps1 -SelfTest` を実行し、終了コード0とsummary JSON生成を確認。
- 実時間2時間soakは短縮不可の実機ゲートとして、正確なコマンド、heartbeat、成果物path、判定条件を文書化。2時間summary取得前はPhase 6を「実装済み・実機soakゲート待ち」と明記する。
- README、getting-started、handoffを現在のPhase 6実装状態へ更新。
- acceptance再監査でLLM/TTS total timeout、Live2D初回失敗後の再boot、service / managed circuit snapshot、bounded enrichment metrics、TTS queue overflow分類をTDD修正し、`ea2bf9e fix(recovery): complete phase 6 acceptance paths`へcommit。
- root scriptとTauri build hookを`corepack pnpm`へ統一。rootとworkspace子processでNode.js `24.15.0` / pnpm `11.11.0`、`Unsupported engine`警告なしを実測。
- 全品質ゲート実測: Rust 274 passed / 7 ignored、frontend 81 passed、fmt、clippy `-D warnings`、typecheck、production build、Tauri debug buildが成功。
- soak SelfTestはexit 0 / `passed=true` / violations・orphan空。RootChild negative testは期待exit 4 / `root_child_rejection.passed=true` / unexpected・orphan空。
- 実時間2時間soakは未実施。Phase 6は「実装済み・実機soakゲート待ち」であり、完了扱いしない。

## 2026-07-14 人間型記憶ライフサイクル

### 実装契約

- SQLite schema versionを7へ更新。v6以前の既存memoryは内容を保持したまま`active`・非pin・mention count 1へ移行し、migration時刻のweight 1.0 `imported` evidenceを1件付与することで、移行直後に休眠化されないgraceを与える。migrationはtransaction内で実行し、再open時は再適用しない。
- strengthは、各evidenceの寄与 `weight * sqrt(30 / max(age_days, 1))` を用い、`user_mention`と`imported`の和を`user`、`recalled`の和を`recalled`として、`user + min(recalled, user * 0.25)`。recall寄与は総strengthの最大20%。strengthが`1.0`未満ならdormant候補で、weight 1.0の言及1/2/3回はそれぞれ30/120/270日まではactive、31/121/271日で閾値未満となる。prompt rankは`0.7 * lexical_relevance + 0.3 * strength`（各値を0〜1へclamp）。
- hybrid classifierは、strictな単一JSON、候補ID allowlist、安全なcontent、元発話に含まれるcontentを検証する。classifier失敗または不正出力時は完全一致候補のreinforceだけを許可し、semantic/destructive fallbackは行わず、それ以外はignore。pinは決定的な明示意図が元発話にある場合だけ許可し、既存候補IDまたは安全な新規contentの片方だけを受け付ける。
- prompt検索対象は`active`のみ。非pin activeがstrength閾値未満になると`dormant`へ遷移し、再言及で`active`へ復帰する。置換元は非pinの`superseded`となりpromptから除外される。非pinの`dormant`/`superseded`は`state_changed_at`から180日経過後に削除対象となり、pin済みmemoryは休眠・削除対象外。

### 受け入れ検証（実測）

- `cargo test -p pw-application memory -- --nocapture`: exit 0（対象17 passed、追加stress 1 passed、失敗0）。
- `cargo test -p pw-storage memory -- --nocapture`: exit 0（対象12 passed、失敗0）。
- `cargo test -p pw-storage v7_upgrade -- --nocapture`: exit 0（対象1 passed、失敗0）。
- `cargo test -p parallel-world-desktop enrichment -- --nocapture`: `SHERPA_ONNX_LIB_DIR`設定、exit 0（対象7 passed、失敗0）。
- `cargo test -p parallel-world-desktop memory_context -- --nocapture`: `SHERPA_ONNX_LIB_DIR`設定、exit 0（対象3 passed、失敗0）。
- `cargo test -p parallel-world-desktop maintenance -- --nocapture`: `SHERPA_ONNX_LIB_DIR`設定、exit 0（対象3 passed、失敗0）。
- `cargo fmt --all --check`: exit 0。
- `cargo clippy --workspace --all-targets -- -D warnings`: `SHERPA_ONNX_LIB_DIR`設定、exit 0。
- `cargo test --workspace`: `SHERPA_ONNX_LIB_DIR`設定、exit 0。全suiteで失敗0。hardware/model/server依存のignore 7件は既知条件どおり。

### Frontend isolation

- 指定範囲 `git diff --name-only 9c92e10..HEAD | Select-String -Pattern '^(apps/desktop/src/|packages/contracts/|apps/desktop/src-tauri/capabilities/|apps/desktop/src-tauri/permissions/)'` は26件一致した。ただし全件が、memory feature開始前に既存Control Center frontendを取り込んだmerge commit `ffac1e2` 由来。
- feature実装baseを除外した `git diff --name-only ffac1e2..HEAD` に同じpatternを適用した結果は0件。したがって人間型記憶ライフサイクルの実差分にfrontend、TypeScript contract、capability、permission変更はない。
